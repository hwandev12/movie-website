<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Your List</title>
</head>
<body>
    <h1>Your List</h1>
    <ul>
        {% for object in your_objects %}
            <li>{{ object }}</li>
        {% endfor %}
    </ul>

    <!-- Pagination links with page numbers -->
    <div class="pagination">
        <span class="step-links">
            {% if your_objects.has_previous %}
                <a href="?page=1">&laquo; first</a>
                <a href="?page={{ your_objects.previous_page_number }}">previous</a>
            {% endif %}

            <!-- Display individual page numbers -->
            {% for num in your_objects.paginator.page_range %}
                {% if num == your_objects.number %}
                    <span class="current">{{ num }}</span>
                {% else %}
                    <a href="?page={{ num }}">{{ num }}</a>
                {% endif %}
            {% endfor %}

            {% if your_objects.has_next %}
                <a href="?page={{ your_objects.next_page_number }}">next</a>
                <a href="?page={{ your_objects.paginator.num_pages }}">last &raquo;</a>
            {% endif %}
        </span>
    </div>
</body>
</html>
